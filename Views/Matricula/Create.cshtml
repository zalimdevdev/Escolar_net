@model Escolar_net.Models.Matricula

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Matricula</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="FechaMatricula" class="control-label"></label>
                <input asp-for="FechaMatricula" class="form-control" />
                <span asp-validation-for="FechaMatricula" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="EstudianteMatricula" class="control-label"></label>
                <input asp-for="EstudianteMatricula" class="form-control" />
                <span asp-validation-for="EstudianteMatricula" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="GradoMatricula" class="control-label"></label>
                <input asp-for="GradoMatricula" class="form-control" />
                <span asp-validation-for="GradoMatricula" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
